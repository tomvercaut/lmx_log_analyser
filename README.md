# LMX logfile analyzer

A library and application to parse and analyse LMX logfiles. The result of the analysis is written to an Excel file.

Build a package and installer to deploy the application:

```shell
gradlew jpackage
```
